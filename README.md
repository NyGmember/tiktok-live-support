# TikTok Live Support System

ระบบสำหรับดึงข้อมูลกิจกรรมจาก TikTok Live (Comment, Like, Gift) มาคำนวณคะแนนและแสดงผลในรูปแบบ Leaderboard และ Overlay แบบ Real-time

## 📋 สารบัญ

- [การติดตั้ง (Installation)](#-การติดตั้ง-installation)
- [การใช้งาน (Usage)](#-การใช้งาน-usage)
- [การตั้งค่า (Configuration)](#-การตั้งค่า-configuration)
- [Libraries ที่สำคัญ](#-libraries-ที่สำคัญ)
- [ข้อตกลงการใช้งาน (Terms of Use)](#-ข้อตกลงการใช้งาน-terms-of-use)

## 🛠 การติดตั้ง (Installation)

```bash
pip install -r requirements.txt
```

4. ตรวจสอบว่ามี Redis และ PostgreSQL รันอยู่

**Frontend:**

1. เข้าไปที่โฟลเดอร์ `frontend`
2. ติดตั้ง dependencies:
   ```bash
   npm install
   ```

## 🚀 การใช้งาน (Usage)

หลังจากรันระบบแล้ว สามารถเข้าใช้งานได้ดังนี้:

- **Frontend (หน้าจอแสดงผล):** `http://localhost:5173`
- **Backend API Docs:** `http://localhost:8000/docs`
- **PGAdmin (จัดการ Database):** `http://localhost:5050`
- **Redis Commander (ดูข้อมูล Redis):** `http://localhost:8081`

### การเริ่มดึงข้อมูล Live

ระบบ Backend จะมี Service สำหรับเชื่อมต่อกับ TikTok Live โดยต้องระบุ `unique_id` ของห้อง Live ที่ต้องการผ่าน API หรือ Config (ขึ้นอยู่กับการ implement ใน `capture_live.py` หรือ API endpoint)

## ⚙️ การตั้งค่า (Configuration)

### Environment Variables

สร้างไฟล์ `.env` ที่ root folder โดยกำหนดค่าดังนี้:

```env
DB_USER=admin
DB_PASSWORD=password
# ค่าอื่นๆ ตามที่ระบุใน docker-compose.yaml
```

### การตั้งค่า Gift Multiplier (ตัวคูณของขวัญ)

ระบบมีการกำหนดตัวคูณคะแนนตามมูลค่าของขวัญ (Coins) โดยสามารถแก้ไข Logic ได้ที่ไฟล์:
`backend/app/services/scoring_service.py`

ค้นหาฟังก์ชัน `_get_gift_multiplier` และปรับแก้ค่าตามต้องการ:

```python
def _get_gift_multiplier(self, coin_value: int) -> int:
    if coin_value <= 4:
        return 5  # คูณ 5
    # ... แก้ไขเงื่อนไขตรงนี้
```

## 📚 Libraries ที่สำคัญ

### Backend

- **[FastAPI](https://fastapi.tiangolo.com/)**: Web framework สำหรับสร้าง API ที่รวดเร็ว
- **[TikTokLive](https://github.com/isaackogan/TikTokLive)**: Library หลักสำหรับเชื่อมต่อและรับ Event จาก TikTok Live
- **[Redis](https://redis.io/)**: ใช้สำหรับ Caching และ Real-time Leaderboard (ZSET)
- **[SQLAlchemy](https://www.sqlalchemy.org/)**: ORM สำหรับจัดการ Database

### Frontend

- **[Vue.js 3](https://vuejs.org/)**: JavaScript Framework สำหรับสร้าง UI
- **[Vite](https://vitejs.dev/)**: Build tool ที่รวดเร็วสำหรับ Frontend
- **[TailwindCSS](https://tailwindcss.com/)**: Utility-first CSS framework สำหรับตกแต่งหน้าตา
- **[Pinia](https://pinia.vuejs.org/)**: State Management สำหรับ Vue

## ⚠️ ข้อตกลงการใช้งาน (Terms of Use)

1. โปรเจกต์นี้จัดทำขึ้นเพื่อการศึกษาและทดสอบระบบเท่านั้น
2. ผู้พัฒนาไม่มีส่วนเกี่ยวข้องกับ TikTok หรือ ByteDance แต่อย่างใด
3. การใช้งาน Library `TikTokLive` ต้องปฏิบัติตามข้อกำหนดของ TikTok และผู้ใช้งานต้องรับความเสี่ยงในการใช้งานเอง (เช่น การถูกจำกัดการเข้าถึงหากใช้งานผิดปกติ)
4. ห้ามนำไปใช้ในทางที่ผิดกฎหมายหรือละเมิดสิทธิส่วนบุคคลของผู้อื่น
