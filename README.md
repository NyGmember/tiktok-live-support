# TikTok Live Support System

ระบบสำหรับดึงข้อมูลกิจกรรมจาก TikTok Live (Comment, Like, Gift) มาคำนวณคะแนนและแสดงผลในรูปแบบ Leaderboard และ Overlay แบบ Real-time เหมาะสำหรับการจัดกิจกรรมไลฟ์สดที่ต้องการการมีส่วนร่วมจากผู้ชม

![Admin Dashboard](snapshot/ck6219ck6219ck62.png)

## 📋 สารบัญ

- [ฟีเจอร์หลัก (Key Features)](#-ฟีเจอร์หลัก-key-features)
- [การติดตั้ง (Installation)](#-การติดตั้ง-installation)
- [การใช้งาน (Usage)](#-การใช้งาน-usage)
- [การตั้งค่า (Configuration)](#-การตั้งค่า-configuration)
- [Libraries ที่สำคัญ](#-libraries-ที่สำคัญ)
- [ข้อตกลงการใช้งาน (Terms of Use)](#-ข้อตกลงการใช้งาน-terms-of-use)

## ✨ ฟีเจอร์หลัก (Key Features)

- **Real-time Leaderboard**: จัดอันดับผู้ร่วมกิจกรรมตามคะแนน (Like, Gift, Comment)
- **Interactive Overlays**: หน้าจอ Overlay สำหรับนำไปซ้อนในโปรแกรม Streaming (OBS, vMix)
  - **Leaderboard Overlay**: แสดงอันดับ 1-5 แบบ Real-time
  - **Question Overlay**: แสดงคอมเมนต์ที่ผู้จัดเลือกขึ้นหน้าจอ พร้อม Transition สวยงาม
- **Session Management**: บันทึกประวัติการไลฟ์ และสามารถ Resume Session เดิมได้
- **Admin Control**: หน้าจอควบคุมสำหรับผู้จัดกิจกรรม (Reset คะแนน, เลือกคอมเมนต์, หยุด/เริ่ม Stream)

## 🛠 การติดตั้ง (Installation)

### Backend

1. เข้าไปที่โฟลเดอร์ `backend`
2. สร้าง Virtual Environment และ Activate:
   ```bash
   python -m venv venv
   # Windows
   venv\Scripts\activate
   # Mac/Linux
   source venv/bin/activate
   ```
3. ติดตั้ง dependencies:
   ```bash
   pip install -r requirements.txt
   ```
4. ตรวจสอบว่ามี Redis และ PostgreSQL รันอยู่ (แนะนำให้ใช้ Docker Compose)

### Frontend

1. เข้าไปที่โฟลเดอร์ `frontend`
2. ติดตั้ง dependencies:
   ```bash
   npm install
   ```

### Docker (Recommended)

สามารถรันทั้งระบบด้วย Docker Compose:

```bash
docker-compose up --build
```

## 🚀 การใช้งาน (Usage)

หลังจากรันระบบแล้ว สามารถเข้าใช้งานได้ดังนี้:

- **Frontend (หน้าจอหลัก):** `http://localhost:5173`
- **Backend API Docs:** `http://localhost:8000/docs`

### 1. Admin Dashboard (หน้าควบคุม)

URL: `http://localhost:5173/`

หน้าจอหลักสำหรับผู้ควบคุมกิจกรรม ประกอบด้วย:

- **Control Panel**: เริ่ม/หยุด Stream, สร้าง Session ใหม่ หรือ Resume Session เดิม
- **Live Leaderboard**: ตารางอันดับผู้เล่นแบบ Real-time
- **User Details**: ดูรายละเอียดผู้เล่นแต่ละคน (ประวัติ Comment, Gift Breakdown)
  - **Reset Score**: สามารถรีเซ็ตคะแนนรายบุคคลได้ (ระบบจะเลือกผู้เล่นลำดับถัดไปขึ้นมาแสดงผลอัตโนมัติ)
  - **Select Comment**: คลิกที่ไอคอน 🛜 เพื่อส่งคอมเมนต์ขึ้นหน้าจอ Question Overlay

![Admin View](snapshot/ck6219ck6219ck62.png)

### 2. Leaderboard Overlay

URL: `http://localhost:5173/overlay`

หน้าจอสำหรับแสดงผลอันดับ 1-5 บน OBS พื้นหลังเป็นสีเขียว (Green Screen) สำหรับ Chroma Key

- แสดง Avatar, ชื่อ, และคะแนน
- มี Animation เมื่ออันดับเปลี่ยนแปลง
- แสดงมงกุฎ 👑 สำหรับอันดับ 1 (เอียง 30 องศา)

![Leaderboard Overlay](<snapshot/ck6219ck6219ck62%20(2).png>)

### 3. Question Overlay

URL: `http://localhost:5173/question`

หน้าจอสำหรับแสดงคอมเมนต์ที่ Admin เลือก พื้นหลังสีเขียว (Green Screen)

- แสดง Avatar, ชื่อ, และข้อความคอมเมนต์
- มี Transition Effect (Pop in/out) เมื่อเปลี่ยนข้อความ

### 4. Session History (ประวัติการไลฟ์)

URL: `http://localhost:5173/sessions`

หน้าจอสำหรับดูประวัติการไลฟ์ย้อนหลัง

- แสดงรายการ Session ทั้งหมด (Start Time, End Time, Total Users)
- สามารถกดดูรายละเอียดของแต่ละ Session ได้ (รายชื่อผู้เล่น, สถิติรวม)
- สามารถกดปุ่ม ▶️ เพื่อ Resume Session เก่ากลับมาใช้งานต่อได้

![Session History](snapshot/2gf1ai2gf1ai2gf1.png)

## ⚙️ การตั้งค่า (Configuration)

### Environment Variables

สร้างไฟล์ `.env` ที่ root folder โดยกำหนดค่าดังนี้:

```env
DB_USER=admin
DB_PASSWORD=password
# ค่าอื่นๆ ตามที่ระบุใน docker-compose.yaml
```

### การตั้งค่า Gift Multiplier (ตัวคูณของขวัญ)

ระบบมีการกำหนดตัวคูณคะแนนตามมูลค่าของขวัญ (Coins) โดยสามารถแก้ไข Logic ได้ที่ไฟล์:
`backend/app/services/scoring_service.py`

ค้นหาฟังก์ชัน `_get_gift_multiplier` และปรับแก้ค่าตามต้องการ:

```python
def _get_gift_multiplier(self, coin_value: int) -> int:
    if coin_value <= 4:
        return 5  # คูณ 5
    # ... แก้ไขเงื่อนไขตรงนี้
```

## 📚 Libraries ที่สำคัญ

### Backend

- **[FastAPI](https://fastapi.tiangolo.com/)**: Web framework สำหรับสร้าง API ที่รวดเร็ว
- **[TikTokLive](https://github.com/isaackogan/TikTokLive)**: Library หลักสำหรับเชื่อมต่อและรับ Event จาก TikTok Live
- **[Redis](https://redis.io/)**: ใช้สำหรับ Caching และ Real-time Leaderboard (ZSET)
- **[SQLAlchemy](https://www.sqlalchemy.org/)**: ORM สำหรับจัดการ Database

### Frontend

- **[Vue.js 3](https://vuejs.org/)**: JavaScript Framework สำหรับสร้าง UI
- **[Vite](https://vitejs.dev/)**: Build tool ที่รวดเร็วสำหรับ Frontend
- **[TailwindCSS](https://tailwindcss.com/)**: Utility-first CSS framework สำหรับตกแต่งหน้าตา
- **[Pinia](https://pinia.vuejs.org/)**: State Management สำหรับ Vue

## ⚠️ ข้อตกลงการใช้งาน (Terms of Use)

1. โปรเจกต์นี้จัดทำขึ้นเพื่อการศึกษาและทดสอบระบบเท่านั้น
2. ผู้พัฒนาไม่มีส่วนเกี่ยวข้องกับ TikTok หรือ ByteDance แต่อย่างใด
3. การใช้งาน Library `TikTokLive` ต้องปฏิบัติตามข้อกำหนดของ TikTok และผู้ใช้งานต้องรับความเสี่ยงในการใช้งานเอง (เช่น การถูกจำกัดการเข้าถึงหากใช้งานผิดปกติ)
4. ห้ามนำไปใช้ในทางที่ผิดกฎหมายหรือละเมิดสิทธิส่วนบุคคลของผู้อื่น
